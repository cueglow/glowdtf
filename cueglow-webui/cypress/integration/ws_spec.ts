describe("Patch Stream", () => { 
    // mock WS connection with mock-socket
    // send subscribe to patch stream example
    // if it is right, the mocked WS returns some example Patch data
    // assert that Patch data shows up in Patch UI
})